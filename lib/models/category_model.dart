import 'package:flutter/material.dart';

// Modelo que representa el nombre de una categoría con su respectivo icono
class Category {
  final IconData icon;
  final String name;

  Category(this.icon, this.name);
}
